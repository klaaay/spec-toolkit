interface:
  display_name: 'MP3 内嵌元数据'
  short_description: '封面、ID3、歌词（网易云优先官方词）与多遍扫描'
  default_prompt: >-
    使用 embed-audio-id3-metadata：对给定音乐库根目录递归处理所有 MP3，写入内嵌封面与 ID3，
    歌词优先网易云平台词；用 --passes 3 跑多遍并把日志里 no_lyrics 减到最少。
